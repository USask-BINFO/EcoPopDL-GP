# Synthetic Test Data

This folder contains small synthetic datasets for quick pipeline checks.

- `chickpea/` contains a diploid PLINK-start test dataset with matching trait and trial files.
- `bnapus/` contains a polyploid raw allele-call test dataset with matching trait and trial files.

These files are artificial and were generated for testing only. They do not represent real field measurements or real genotype observations.
